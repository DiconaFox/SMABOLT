<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.StudentsMobileAssistant" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blueBiruza</item>
        <item name="colorPrimaryVariant">@color/blueBiruza</item>
        <item name="colorOnPrimary">@color/warmWhite</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/biruza</item>
        <item name="colorSecondaryVariant">@color/spruce</item>
        <item name="colorOnSecondary">@color/warmBlack</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/blueBiruza</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.StudentsMobileAssistant" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

</resources>